# 更新日志

## 0.2.0 — 2026-09-15

### 新增
- **恢复误删项目**：菜单 `File ▸ Restore deleted project…`，从 `.trash` 中选一个项目还原（保留最近 10 个）。
- **画布空白处双击直接新建便签**，位置即鼠标落点。
- **命令行参数**：`--data-dir` 指定数据目录，`--version` 查看版本。
- 帮助菜单新增 `Open data folder`，一键在文件管理器里打开数据目录。
- 补充 `README.md`、`requirements.txt`、`run.bat` 启动脚本。

### 修复
- 修正 0.1.0 中任务列表筛选会把任务挑错的问题：每一行现在都绑定自己的任务 id（按索引匹配改为按 id 匹配）。
- 画布 `QFrame` 未导入导致绘制失败的问题。
- 撤销快捷键从 `Ctrl+Z` 调整为 `Ctrl+Shift+Z`，避免与文本框撤销冲突。
- 移除键盘事件里无效的 `event.matches(...)` 分支。

### 改进
- 画布：缩放、平移、适应内容、网格开关、整理便签、删除撤销栈。
- 便签：颜色、字号、标签胶囊、缩放手柄、复制、搜索定位高亮、标签过滤淡化。
- 思维导图：父子节点、平滑贝塞尔连线、重命名、节点配色、删除子树、自动尺寸。
- 侧栏：悬停展开 + 图钉固定、项目快速切换、日历到期圆点、任务过滤、优先级、行内编辑、截止日期。
- 数据安全：崩溃安全写入与 `.bak` 兜底、输入防抖保存、20 秒自动保存、窗口几何记忆。
- 搜索（`Ctrl+F`）覆盖便签正文、标签、导图节点。
- 项目导出 JSON / Markdown。
